#ifndef REVERSE_VECTOR_H
#define REVERSE_VECTOR_H

#include <iostream>
#include <string>
#include <vector>

using namespace std;

vector<int> reverse(const vector<int> &v);
void readVectorAlt(vector<int> &);
void printVector(const vector<int> &);

#endif